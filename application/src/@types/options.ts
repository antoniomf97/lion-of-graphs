export type options = {
    color: string,
    title: {
        label: string,
        color: string,
        fontsize: number
    },
    xlabel: {
        xlabel: string,
        loc: string
    },
    ylabel: {
        ylabel: string,
        loc: string
    },
    grid: {
        visible: boolean,
        axis: string
    }
};
